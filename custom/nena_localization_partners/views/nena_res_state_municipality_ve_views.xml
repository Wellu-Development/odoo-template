<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Vista tipo formulario -->
    <record id="view_form_nena_res_state_municipality_ve" model="ir.ui.view">
        <field name="name">nena.res.state.municipality.ve.form</field>
        <field name="model">nena.res.state.municipality.ve</field>
        <field name="arch" type="xml">
            <form string="Localized Municipality">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="state_id"/>
                        <field name="code"/>
                        <field name="ref"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Vista tipo lista -->
    <record id="view_list_nena_res_state_municipality_ve" model="ir.ui.view">
        <field name="name">nena.res.state.municipality.ve.list</field>
        <field name="model">nena.res.state.municipality.ve</field>
        <field name="arch" type="xml">
            <list string="Localized Municipalities">
                <field name="name"/>
                <field name="state_id"/>
                <field name="code"/>
                <field name="ref"/>
            </list>
        </field>
    </record>

    <!-- Acción -->
    <record id="action_nena_res_state_municipality_ve" model="ir.actions.act_window">
        <field name="name">Localized Municipalities</field>
        <field name="res_model">nena.res.state.municipality.ve</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Menú -->
    <menuitem id="menu_nena_res_state_municipality_ve" name="Municipalities"
              parent="menu_nena_localization_root"
              action="action_nena_res_state_municipality_ve"
              sequence="30"/>
</odoo>